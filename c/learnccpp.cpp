#include <iostream>
// #include <bits/stdc++.h>
// this includes all the libraries
using namespace std; // this makes it so that we don't have to write std::cout every time
int main()
{
    // std::cout << "Hey Striver!";
    // cout << "Hey Striver!" << "\n"; // faster
    // cout << "Hey Striver!" << endl; // adds a new line character
    // float x;
    // cin >> x; // cin is the input stream object
    // cout << "The value of x is " << x << endl;
    // cin.ignore(); // Clear the newline character from the buffer
    // string str;
    // getline(cin, str); // this is used to get the entire line
    // cout << str;
    // char ch = 'g';

    for (int i = 0; i < 5; i++)
    {
        cout << "Hello World" << endl;
    }

    return 0;
}